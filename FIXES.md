# FIXES.md — фиксы пост-мерджа `teammate/main`

Документ ведётся Flutter-Claude в сессии Мансура. Каждый фикс описан так, чтобы
Мансур мог самостоятельно протестировать его, а Backend-Claude (в другой
сессии) — понять, что нужно дополнить на бэке.

**Контекст:** защита диплома **2026-06-08**. Мерж от 2026-05-27
(Flutter `819931f`, Backend `094ba13`). Все фиксы ниже сделаны 2026-05-27 после
первого тестирования.

---

## Фикс 1 — `parking_spot_id` обязателен для гостя на авто

**Файл:** `lib/pages/guests_page.dart`
**Коммит Flutter:** `f15ddff`
**Бэк:** изменений нет (поле уже принимается).

### Что сделано
- При включённом переключателе **«Гость на авто»** выбор гостевого места стал обязательным.
- Подпись поля изменена с «Гостевое место (необязательно)» → **«Гостевое место (обязательно)»** жирным.
- Кнопка «Создать пропуск» **disabled**, на ней надпись «Выберите место для авто», пока место не выбрано.
- В payload `POST /guest-access` теперь всегда уходит `parking_spot_id` при `access_type=car` (раньше уходил только если выбран).

### Как протестировать
1. Зайти резидентом → Гости.
2. Включить переключатель «Гость на авто».
3. **НЕ** выбирая место, попробовать нажать «Создать пропуск».
4. ✅ Кнопка disabled, на ней «Выберите место для авто».
5. Выбрать любое зелёное место.
6. ✅ Кнопка стала активной с надписью «Создать пропуск».
7. Заполнить остальные поля, создать пропуск.
8. ✅ В списке пропусков появился новый, привязанный к месту.

---

## Фикс 2 — push `parking_no_permit` если гостевое место заняли, а гость не приехал

**Файлы Flutter:** `lib/services/notifications_service.dart`, `lib/pages/notifications_page.dart`
**Коммит Flutter:** `0e6edfa`
**Файл бэка:** `internal/mqtt/client.go`
**Коммит бэка:** `8721742`

### Что сделано на бэке
Переименование `kind=parking_alert` → `kind=parking_no_permit` для гостевого
сценария (4 точки записи: notification резиденту, notification админу, FCM резиденту, FCM админу).

### Что сделано на фронте
Полное покрытие `parking_no_permit` во всех ветках пайплайна уведомлений:
- `_isAndroidNativeForegroundKind` — теперь показывается как нативное Android-уведомление в foreground.
- `_emitIfSensorAlert` — `ParkingService.refreshFromPush()` дёргается и для этого kind.
- `_emitInApp` — in-app баннер с title/body при получении в foreground.
- `_payloadToMessage` — корректный парсинг тапнутого уведомления.
- `notifications_page.dart` — иконка `Icons.no_meeting_room` красная (уже была) + добавлена иконка для `parking_spot_freed` (синий `local_parking`).

### Финальный контракт `kind`
| kind | Когда | Иконка |
|---|---|---|
| `parking_no_permit` | Гостевое + active-пропуск + чужак приехал раньше гостя | 🔴 `no_meeting_room` |
| `parking_alert` | Permanent занято, владелец не проезжал через ворота 30 мин | 🟠 `local_parking` |
| `parking_spot_freed` | Освобождение (permanent freed / гостевое — чужак уехал) | 🔵 `local_parking` |

### Как протестировать
**Сценарий A (positive — чужак на гостевом месте):**
1. Резидент → создать гостевой пропуск с привязкой к месту `G-01`.
2. QR **не сканировать** (статус пропуска = `active`).
3. В Node-RED дёрнуть инжект **Guest spot occupied G-01**.
4. ✅ На телефон резидента приходит push «🚫 Нет доступа в паркинг».
5. ✅ В истории уведомлений — красная иконка `no_meeting_room`.
6. ✅ Если приложение открыто в foreground — снизу баннер с тем же сообщением.

**Сценарий B (negative — гость законно приехал):**
1. Создать пропуск на `G-01`.
2. Охранник сканирует QR (или Backend Claude дёргает бэк вручную) → статус `arrived`.
3. Node-RED повторно шлёт **Guest spot occupied G-01**.
4. ✅ Push **НЕ приходит**.

---

## Фикс 3 — не уведомлять и не показывать «Сообщить» если резидент сам занял своё место

**Файл Flutter:** `lib/pages/guests_page.dart`
**Коммит Flutter:** `f15ddff`
**Бэк:** **изменений не потребовалось** (логика уже корректна, см. ниже).

### Что сделано на фронте
Раньше `_myActivePassSpotIds` объединял пропуска со статусами `active` + `arrived`
— из-за этого даже после законного приезда гостя место в сетке показывалось
как «моё занятое» и предлагало «Сообщить администратору».

Теперь разделено на два множества:
- `_pendingPassSpotIds` — только статус `active` (QR ещё не сканировали).
- `_arrivedPassSpotIds` — только статус `arrived` (гость уже на территории).

Логика тапа по красному месту в сетке:
- Если место в `_pendingPassSpotIds` → диалог «Гостевое место занято / Сообщить администратору».
- Если место в `_arrivedPassSpotIds` → снэкбар «Место занято вашим гостем (на территории)», без диалога.

### Почему на бэке ничего не делали (от Backend Claude, дословно)

> Логика уже корректна (`client.go:421-435`): для permanent-места при occupied
> бэк проверяет был ли barrier_event с машиной владельца через parking-gate за
> последние 30 минут. Если был — push не шлётся.
>
> Симптом «ложных пушей при симуляции» — это не баг бэка, а неполная симуляция.
> Текущий Node-RED parking flow шлёт только `{spot_number, event_type}`, никакого
> car_number в payload нет. В реальной жизни машина владельца не может
> оказаться на permanent-месте, не проехав через ворота. При симуляции Мансур
> инжектит сразу parking occupied без предварительного barrier-gate IN —
> поэтому бэк справедливо считает машину чужой.

### Как протестировать (правильный сценарий симуляции)

**Сценарий C (positive — владелец на своём permanent-месте):**
1. Открыть Node-RED → вкладка **Barrier**.
2. Инжект **Manual IN** с номером владельца (например `A777BC`).
   - ⚠️ Проверить, что `gate_id = parking-gate`, а **не** `main-gate`. Если такого инжекта нет — попросить Егора добавить.
3. Подождать **30 секунд** (бэк регистрирует barrier_event).
4. Node-RED → вкладка **Parking Simulator** → **Permanent spot occupied P-01** (где `P-01` закреплено за владельцем `A777BC`).
5. ✅ Push **НЕ приходит**, в истории уведомлений ничего нового.

**Сценарий D (negative — чужак на permanent-месте):**
1. Без предварительного barrier IN дёрнуть **P-01 occupied**.
2. ✅ Приходит push с `kind=parking_alert` (оранжевая иконка `local_parking`).

**Сценарий E (UI фронта — гость уже приехал):**
1. Создать пропуск на `G-01`.
2. QR-скан (статус `arrived`).
3. Открыть страницу «Гости» → форма создания → сетка гостевых мест.
4. Место `G-01` красное (занято).
5. Тапнуть на `G-01`.
6. ✅ Появляется снэкбар «Место G-01 занято вашим гостем (на территории)».
7. ✅ Диалог «Сообщить администратору» **НЕ появляется**.

---

## Фикс 4 — добавление сотрудника из админ-панели

**Файл Flutter:** `lib/pages/admin_staff_page.dart`
**Коммит Flutter:** `5238236`
**Бэк:** **нужна реализация эндпоинта** (см. ниже).

### Что сделано на фронте

1. В `AppBar` страницы «Сотрудники» (видна только админу) добавлена кнопка с иконкой `Icons.person_add_alt_1` справа.
2. По тапу открывается полноэкранная форма **«Новый сотрудник»** (`_CreateStaffPage`) со следующими полями:

| Поле | Тип | Валидация | API ключ |
|---|---|---|---|
| ФИО | text | required | `full_name` |
| Специальность | dropdown | required (default `plumbing`) | `specialty` |
| Email (для входа) | email | required + regex `*@*.*` | `email` |
| Пароль | password + toggle visibility | required + min 6 символов | `password` |
| Телефон | phone | required | `phone` |
| Режим работы | text (default «Пн–Пт, 09:00–18:00») | required | `work_schedule` |
| Описание | text multiline 3 строки | optional | `description` |

3. Значения `specialty` — те же, что использует `specialtyLabel()`:
   `plumbing`, `electrical`, `cleaning`, `garbage`, `intercom`, `elevator`.
4. После успешного создания: снэкбар «Сотрудник добавлен», возврат на список, автоматический `_load()`.
5. При ошибке: снэкбар с текстом ошибки через `friendlyError()`.

### Что нужно сделать Backend Claude — `POST /api/v1/admin/staff`

**Эндпоинт:** `POST /api/v1/admin/staff`
**Авторизация:** только роль `admin` (как `GET /admin/staff`).
**Content-Type:** `application/json`

**Тело запроса:**
```json
{
  "full_name": "Ахмет Бекжанов",
  "email": "plumber@test.com",
  "password": "12341234",
  "phone": "+7 701 234 5678",
  "specialty": "plumbing",
  "work_schedule": "Пн–Пт, 09:00–18:00",
  "description": "Устранение протечек, ремонт кранов и труб."
}
```

**Ожидаемое поведение бэка:**
1. Валидация: все поля кроме `description` — обязательные.
2. Проверка уникальности `email` (если занят — 409 `{"error":"email_taken"}`).
3. Проверка `specialty` ∈ {`plumbing`, `electrical`, `cleaning`, `garbage`, `intercom`, `elevator`} (иначе 400).
4. Хэширование пароля (bcrypt, как у обычных юзеров).
5. Создание записи в таблице сотрудников (`profiles` с `role='staff'` + специализация в отдельной таблице/колонке — на твоё усмотрение, главное чтобы `GET /admin/staff` сразу её увидел).
6. Возврат **201** с телом созданного сотрудника в том же формате, что отдаёт `GET /admin/staff` (для удобства; фронт сейчас не использует тело, делает `_load()` повторно — но всё же).

**Что важно:**
- Созданный сотрудник должен **сразу залогиниваться** обычным `POST /auth/login` с `email`+`password` (как и предусмотрено в форме «Email для входа»).
- В таблице сотрудников должны быть колонки `work_schedule` и `description` (если их ещё нет — миграция). Их отображение в `GET /admin/staff` и в `services_page.dart` — отдельная задача (см. «Опциональное продолжение» ниже).

**Возможные коды ответов:**
- `201 Created` — успех
- `400 Bad Request` — невалидная специальность, кривой email, короткий пароль
- `409 Conflict` — email уже занят
- `403 Forbidden` — не админ

### Опциональное продолжение (если останется время)

Сейчас `services_page.dart` использует **захардкоженный массив `_kServices`** с данными сотрудников (`staffName`, `phone`, `hours`, `description`). Это значит, что новые сотрудники, добавленные через форму, **не попадут** на вкладку «Сервисы» у резидента, пока этот список не переедет в API.

**Что нужно сделать (отдельная задача):**
1. Бэк добавляет эндпоинт `GET /services` (или возвращает поля `work_schedule`, `description`, `phone` в существующем `GET /admin/staff` и/или `GET /staff`).
2. Фронт меняет `_kServices` в `services_page.dart` на загрузку из API.

Это **не блокер для защиты** — для демо комиссии достаточно показать, что админ может добавить сотрудника через форму, и он появится в списке `GET /admin/staff` (вкладка «Сотрудники» в админ-панели).

### Как протестировать (когда бэк готов)

1. Залогиниться админом.
2. Открыть Дашборд → раздел «Сотрудники».
3. ✅ В правом верхнем углу видна иконка `+` (person_add).
4. Тапнуть.
5. ✅ Открылась форма «Новый сотрудник».
6. Заполнить:
   - ФИО: `Иван Тестов`
   - Специальность: `Электрик`
   - Email: `electrician@test.com`
   - Пароль: `123456`
   - Телефон: `+7 707 111 22 33`
   - Режим работы: `Пн–Пт, 09:00–18:00`
   - Описание: `Тестовый сотрудник для защиты`
7. ✅ Тап «Создать сотрудника» → снэкбар «Сотрудник добавлен», возврат на список.
8. ✅ В списке появилась карточка «Иван Тестов / Электрик / 0 в работе / 0 сегодня».
9. Логаут → попытаться войти как `electrician@test.com` / `123456`.
10. ✅ Логин успешен, открывается интерфейс сотрудника.
11. **Edge case:** попытаться создать второго с тем же email → ✅ снэкбар «Email уже занят» (или похожий, в зависимости от текста friendlyError).
12. **Edge case:** пароль из 3 символов → ✅ под полем «Минимум 6 символов», submit не происходит.

---

## Общие чек-листы (после ВСЕХ фиксов)

### Регрессии — что не должно сломаться
- [ ] Логин/регистрация работают как раньше.
- [ ] Существующие гостевые пропуска без `parking_spot_id` (созданные до Фикса 1) **не падают** в UI.
- [ ] FCM-уведомления с `kind=parking_alert` (permanent-сценарий) приходят и обрабатываются.
- [ ] Старая логика «Сообщить администратору» в `guests_page.dart` для **active** пропусков работает.
- [ ] Список сотрудников `GET /admin/staff` грузится корректно после добавления нового сотрудника.

### Финальная демо-цепочка для защиты (3 минуты)
1. **Auth:** регистрация в 4 шага → логин.
2. **IoT:** Node-RED Water Alert → Dashboard «Тревога!» → принять → подтвердить → решить.
3. **Parking attempts:** 3 отказа резиденту → апелляция → resolve.
4. **Guest QR:** создать пропуск с местом → 1 скан → «На территории» → 2 скан → «Использован».
5. **Admin verification:** просмотр документа → одобрение.
6. **(Новое)** **Создание сотрудника:** админ → Сотрудники → `+` → форма → создание → логин нового сотрудника.

---

## Журнал коммитов Flutter

| Дата | Коммит | Что |
|---|---|---|
| 2026-05-27 | `4224060` | Add Flutter demo notes for diploma defense |
| 2026-05-27 | `819931f` | Merge teammate/main: parking attempts, appeal flow, guest spot binding |
| 2026-05-27 | `f15ddff` | Фиксы 1 и 3: обязательное место + умная логика «Сообщить» |
| 2026-05-27 | `0e6edfa` | Фикс 2: покрытие `parking_no_permit` в notifications_service |
| 2026-05-27 | `5238236` | Фикс 4: форма создания сотрудника в admin_staff_page |

## Журнал коммитов Backend (по словам Backend Claude)

| Дата | Коммит | Что |
|---|---|---|
| 2026-05-27 | `094ba13` | Merge: 3 попытки, апелляция, parking_permit_status, гостевой parking_spot_id |
| 2026-05-27 | `8721742` | Фикс 2: переименование `parking_alert` → `parking_no_permit` (mqtt/client.go) |
| TBD | TBD | Фикс 4: `POST /admin/staff` эндпоинт |

---

**Поддерживается Flutter-Claude в сессии Мансура. При появлении нового фикса —
дописывать новую секцию по тому же шаблону: что сделано / что нужно от бэка / как протестировать.**
