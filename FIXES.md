# FIXES.md — фиксы пост-мерджа `teammate/main`

Документ ведётся Flutter-Claude в сессии Мансура. Каждый фикс описан так, чтобы
Мансур мог самостоятельно протестировать его, а Backend-Claude (в другой
сессии) — понять, что нужно дополнить на бэке.

**Контекст:** защита диплома **2026-06-08**. Мерж от 2026-05-27
(Flutter `819931f`, Backend `094ba13`). Все фиксы ниже сделаны 2026-05-27 после
первого тестирования.

---

## Фикс 1 — `parking_spot_id` обязателен для гостя на авто

**Файл:** `lib/pages/guests_page.dart`
**Коммит Flutter:** `f15ddff`
**Бэк:** изменений нет (поле уже принимается).

### Что сделано
- При включённом переключателе **«Гость на авто»** выбор гостевого места стал обязательным.
- Подпись поля изменена с «Гостевое место (необязательно)» → **«Гостевое место (обязательно)»** жирным.
- Кнопка «Создать пропуск» **disabled**, на ней надпись «Выберите место для авто», пока место не выбрано.
- В payload `POST /guest-access` теперь всегда уходит `parking_spot_id` при `access_type=car` (раньше уходил только если выбран).

### Как протестировать
1. Зайти резидентом → Гости.
2. Включить переключатель «Гость на авто».
3. **НЕ** выбирая место, попробовать нажать «Создать пропуск».
4. ✅ Кнопка disabled, на ней «Выберите место для авто».
5. Выбрать любое зелёное место.
6. ✅ Кнопка стала активной с надписью «Создать пропуск».
7. Заполнить остальные поля, создать пропуск.
8. ✅ В списке пропусков появился новый, привязанный к месту.

---

## Фикс 2 — push `parking_no_permit` если гостевое место заняли, а гость не приехал

**Файлы Flutter:** `lib/services/notifications_service.dart`, `lib/pages/notifications_page.dart`
**Коммит Flutter:** `0e6edfa`
**Файл бэка:** `internal/mqtt/client.go`
**Коммит бэка:** `8721742`

### Что сделано на бэке
Переименование `kind=parking_alert` → `kind=parking_no_permit` для гостевого
сценария (4 точки записи: notification резиденту, notification админу, FCM резиденту, FCM админу).

### Что сделано на фронте
Полное покрытие `parking_no_permit` во всех ветках пайплайна уведомлений:
- `_isAndroidNativeForegroundKind` — теперь показывается как нативное Android-уведомление в foreground.
- `_emitIfSensorAlert` — `ParkingService.refreshFromPush()` дёргается и для этого kind.
- `_emitInApp` — in-app баннер с title/body при получении в foreground.
- `_payloadToMessage` — корректный парсинг тапнутого уведомления.
- `notifications_page.dart` — иконка `Icons.no_meeting_room` красная (уже была) + добавлена иконка для `parking_spot_freed` (синий `local_parking`).

### Финальный контракт `kind`
| kind | Когда | Иконка |
|---|---|---|
| `parking_no_permit` | Гостевое + active-пропуск + чужак приехал раньше гостя | 🔴 `no_meeting_room` |
| `parking_alert` | Permanent занято, владелец не проезжал через ворота 30 мин | 🟠 `local_parking` |
| `parking_spot_freed` | Освобождение (permanent freed / гостевое — чужак уехал) | 🔵 `local_parking` |

### Как протестировать
**Сценарий A (positive — чужак на гостевом месте):**
1. Резидент → создать гостевой пропуск с привязкой к месту `G-01`.
2. QR **не сканировать** (статус пропуска = `active`).
3. В Node-RED дёрнуть инжект **Guest spot occupied G-01**.
4. ✅ На телефон резидента приходит push «🚫 Нет доступа в паркинг».
5. ✅ В истории уведомлений — красная иконка `no_meeting_room`.
6. ✅ Если приложение открыто в foreground — снизу баннер с тем же сообщением.

**Сценарий B (negative — гость законно приехал):**
1. Создать пропуск на `G-01`.
2. Охранник сканирует QR (или Backend Claude дёргает бэк вручную) → статус `arrived`.
3. Node-RED повторно шлёт **Guest spot occupied G-01**.
4. ✅ Push **НЕ приходит**.

---

## Фикс 3 — не уведомлять и не показывать «Сообщить» если резидент сам занял своё место

**Файл Flutter:** `lib/pages/guests_page.dart`
**Коммит Flutter:** `f15ddff`
**Бэк:** **изменений не потребовалось** (логика уже корректна, см. ниже).

### Что сделано на фронте
Раньше `_myActivePassSpotIds` объединял пропуска со статусами `active` + `arrived`
— из-за этого даже после законного приезда гостя место в сетке показывалось
как «моё занятое» и предлагало «Сообщить администратору».

Теперь разделено на два множества:
- `_pendingPassSpotIds` — только статус `active` (QR ещё не сканировали).
- `_arrivedPassSpotIds` — только статус `arrived` (гость уже на территории).

Логика тапа по красному месту в сетке:
- Если место в `_pendingPassSpotIds` → диалог «Гостевое место занято / Сообщить администратору».
- Если место в `_arrivedPassSpotIds` → снэкбар «Место занято вашим гостем (на территории)», без диалога.

### Почему на бэке ничего не делали (от Backend Claude, дословно)

> Логика уже корректна (`client.go:421-435`): для permanent-места при occupied
> бэк проверяет был ли barrier_event с машиной владельца через parking-gate за
> последние 30 минут. Если был — push не шлётся.
>
> Симптом «ложных пушей при симуляции» — это не баг бэка, а неполная симуляция.
> Текущий Node-RED parking flow шлёт только `{spot_number, event_type}`, никакого
> car_number в payload нет. В реальной жизни машина владельца не может
> оказаться на permanent-месте, не проехав через ворота. При симуляции Мансур
> инжектит сразу parking occupied без предварительного barrier-gate IN —
> поэтому бэк справедливо считает машину чужой.

### Как протестировать (правильный сценарий симуляции)

**Сценарий C (positive — владелец на своём permanent-месте):**
1. Открыть Node-RED → вкладка **Barrier**.
2. Инжект **Manual IN** с номером владельца (например `A777BC`).
   - ⚠️ Проверить, что `gate_id = parking-gate`, а **не** `main-gate`. Если такого инжекта нет — попросить Егора добавить.
3. Подождать **30 секунд** (бэк регистрирует barrier_event).
4. Node-RED → вкладка **Parking Simulator** → **Permanent spot occupied P-01** (где `P-01` закреплено за владельцем `A777BC`).
5. ✅ Push **НЕ приходит**, в истории уведомлений ничего нового.

**Сценарий D (negative — чужак на permanent-месте):**
1. Без предварительного barrier IN дёрнуть **P-01 occupied**.
2. ✅ Приходит push с `kind=parking_alert` (оранжевая иконка `local_parking`).

**Сценарий E (UI фронта — гость уже приехал):**
1. Создать пропуск на `G-01`.
2. QR-скан (статус `arrived`).
3. Открыть страницу «Гости» → форма создания → сетка гостевых мест.
4. Место `G-01` красное (занято).
5. Тапнуть на `G-01`.
6. ✅ Появляется снэкбар «Место G-01 занято вашим гостем (на территории)».
7. ✅ Диалог «Сообщить администратору» **НЕ появляется**.

---

## Фикс 4 — добавление сотрудника из админ-панели ✅ ГОТОВ

**Файл Flutter:** `lib/pages/admin_staff_page.dart`
**Коммит Flutter:** `5238236`
**Файл бэка:** `internal/handler/staff.go` + миграция `014_staff_profile_fields.sql`
**Коммит бэка:** `c3309ca`

### Что сделано на фронте

1. В `AppBar` страницы «Сотрудники» (видна только админу) добавлена кнопка с иконкой `Icons.person_add_alt_1` справа.
2. По тапу открывается полноэкранная форма **«Новый сотрудник»** (`_CreateStaffPage`) со следующими полями:

| Поле | Тип | Валидация | API ключ |
|---|---|---|---|
| ФИО | text | required | `full_name` |
| Специальность | dropdown | required (default `plumbing`) | `specialty` |
| Email (для входа) | email | required + regex `*@*.*` | `email` |
| Пароль | password + toggle visibility | required + min 6 символов | `password` |
| Телефон | phone | required | `phone` |
| Режим работы | text (default «Пн–Пт, 09:00–18:00») | required | `work_schedule` |
| Описание | text multiline 3 строки | optional | `description` |

3. Значения `specialty` — те же, что использует `specialtyLabel()`:
   `plumbing`, `electrical`, `cleaning`, `garbage`, `intercom`, `elevator`.
4. После успешного создания: снэкбар «Сотрудник добавлен», возврат на список, автоматический `_load()`.
5. При ошибке: снэкбар с текстом ошибки через `friendlyError()`.

### Что реализовано на бэке (от Backend Claude, c3309ca)

- Миграция `014_staff_profile_fields.sql` — добавляет `profiles.work_schedule VARCHAR(255)` и `profiles.description TEXT`. Применится автоматически при следующем старте бэка.
- `POST /api/v1/admin/staff`:
  - Только `role=admin`.
  - Валидация: email-формат, password ≥ 6, specialty из 6 значений.
  - bcrypt-хэш пароля, `INSERT INTO users + profiles` в одной транзакции.
  - Созданному сотруднику ставится `role='staff'`, `verification_status='approved'` (не висит в очереди верификации).
  - `409 Conflict` на дубликат email, `201 Created` с телом созданного сотрудника.
- **Логин сотрудника работает из коробки**: `POST /auth/login` с email/паролем → JWT с `role='staff'`. Никаких отдельных staff-auth эндпоинтов не нужно.
- **Фронт уже поддерживает роль `staff`** (`dashboard_page.dart:148-198`): сотруднику показывается дашборд с 2 вкладками — «Заявки» (ServiceRequestsPage) и «Профиль» (ProfilePage). Никаких правок Flutter после Backend-фикса не потребовалось.

### Контракт API (зафиксирован)

**Эндпоинт:** `POST /api/v1/admin/staff`
**Авторизация:** только роль `admin` (как `GET /admin/staff`).
**Content-Type:** `application/json`

**Тело запроса:**
```json
{
  "full_name": "Ахмет Бекжанов",
  "email": "plumber@test.com",
  "password": "12341234",
  "phone": "+7 701 234 5678",
  "specialty": "plumbing",
  "work_schedule": "Пн–Пт, 09:00–18:00",
  "description": "Устранение протечек, ремонт кранов и труб."
}
```

**Ожидаемое поведение бэка:**
1. Валидация: все поля кроме `description` — обязательные.
2. Проверка уникальности `email` (если занят — 409 `{"error":"email_taken"}`).
3. Проверка `specialty` ∈ {`plumbing`, `electrical`, `cleaning`, `garbage`, `intercom`, `elevator`} (иначе 400).
4. Хэширование пароля (bcrypt, как у обычных юзеров).
5. Создание записи в таблице сотрудников (`profiles` с `role='staff'` + специализация в отдельной таблице/колонке — на твоё усмотрение, главное чтобы `GET /admin/staff` сразу её увидел).
6. Возврат **201** с телом созданного сотрудника в том же формате, что отдаёт `GET /admin/staff` (для удобства; фронт сейчас не использует тело, делает `_load()` повторно — но всё же).

**Что важно:**
- Созданный сотрудник должен **сразу залогиниваться** обычным `POST /auth/login` с `email`+`password` (как и предусмотрено в форме «Email для входа»).
- В таблице сотрудников должны быть колонки `work_schedule` и `description` (если их ещё нет — миграция). Их отображение в `GET /admin/staff` и в `services_page.dart` — отдельная задача (см. «Опциональное продолжение» ниже).

**Возможные коды ответов:**
- `201 Created` — успех
- `400 Bad Request` — невалидная специальность, кривой email, короткий пароль
- `409 Conflict` — email уже занят
- `403 Forbidden` — не админ

### Опциональное продолжение (если останется время)

Сейчас `services_page.dart` использует **захардкоженный массив `_kServices`** с данными сотрудников (`staffName`, `phone`, `hours`, `description`). Это значит, что новые сотрудники, добавленные через форму, **не попадут** на вкладку «Сервисы» у резидента, пока этот список не переедет в API.

**Что нужно сделать (отдельная задача):**
1. Бэк добавляет эндпоинт `GET /services` (или возвращает поля `work_schedule`, `description`, `phone` в существующем `GET /admin/staff` и/или `GET /staff`).
2. Фронт меняет `_kServices` в `services_page.dart` на загрузку из API.

Это **не блокер для защиты** — для демо комиссии достаточно показать, что админ может добавить сотрудника через форму, и он появится в списке `GET /admin/staff` (вкладка «Сотрудники» в админ-панели).

### Как протестировать (когда бэк готов)

1. Залогиниться админом.
2. Открыть Дашборд → раздел «Сотрудники».
3. ✅ В правом верхнем углу видна иконка `+` (person_add).
4. Тапнуть.
5. ✅ Открылась форма «Новый сотрудник».
6. Заполнить:
   - ФИО: `Иван Тестов`
   - Специальность: `Электрик`
   - Email: `electrician@test.com`
   - Пароль: `123456`
   - Телефон: `+7 707 111 22 33`
   - Режим работы: `Пн–Пт, 09:00–18:00`
   - Описание: `Тестовый сотрудник для защиты`
7. ✅ Тап «Создать сотрудника» → снэкбар «Сотрудник добавлен», возврат на список.
8. ✅ В списке появилась карточка «Иван Тестов / Электрик / 0 в работе / 0 сегодня».
9. Логаут → попытаться войти как `electrician@test.com` / `123456`.
10. ✅ Логин успешен, открывается интерфейс сотрудника.
11. **Edge case:** попытаться создать второго с тем же email → ✅ снэкбар «Email уже занят» (или похожий, в зависимости от текста friendlyError).
12. **Edge case:** пароль из 3 символов → ✅ под полем «Минимум 6 символов», submit не происходит.

---

---

## Фикс 5 — динамический список сотрудников на странице «Сервисы» ✅ ГОТОВ

**Файлы Flutter:**
- `lib/services/staff_service.dart` (новый)
- `lib/pages/services_page.dart` (переписан)

**Коммит Flutter:** `204694c`
**Файл бэка:** `internal/handler/staff.go` (хэндлер `PublicList`)
**Коммит бэка:** `c3309ca`

### Контекст
Раньше на вкладке «Сервисы» у резидента отображался захардкоженный список сотрудников (`_kServices` в `services_page.dart`). Добавление сотрудника через Фикс 4 не отражалось на этой вкладке. Этот фикс делает список сервисов динамическим.

### Что сделано на фронте

1. **`lib/services/staff_service.dart`** — новый сервис с моделью `StaffMember` и методом `getStaff()` (дёргает `GET /staff`).
2. **`lib/pages/services_page.dart`** переписан:
   - `ServicesPage` теперь `StatefulWidget` с `_load()`.
   - UI-метаданные (название карточки, подзаголовок, иконка, категории заявок) остались в коде в карте `_kSpecialtyMeta` (индекс — `specialty`).
   - Живые данные (ФИО, телефон, режим работы, описание) подтягиваются из API.
   - Маппинг: для каждой специальности из `_kSpecialtyMeta` берётся **первый** сотрудник этой специальности из ответа `/staff`. Если нет — карточка показывается с надписью «Сотрудник не назначен», `available=false`, телефон/часы — прочерк.
   - Если запрос упал — fallback: все карточки как «не назначен» + жёлтый баннер с текстом ошибки.
   - Добавлен `RefreshIndicator` (свайп вниз).
3. **Карточка «Охрана»** оставлена статичной (`_kSecurityService`) — у неё нет отдельной специальности, она информационная.

### Что реализовано на бэке (от Backend Claude, c3309ca)

- `GET /api/v1/staff`:
  - Авторизация: любой авторизованный пользователь.
  - Возвращает массив `publicStaffMember` ровно в формате `StaffMember.fromMap`.
  - **`is_available` вычисляется автоматически**: `true` если у сотрудника нет заявок со статусом `in_progress`, иначе `false`. Поле не хранится в БД, обновляется само при назначении/закрытии заявок админом.
- Разные хэндлеры `/admin/staff` (со счётчиками `in_progress_count` / `done_today_count`) и `/staff` (с `work_schedule` / `description` / `is_available`) — это интенционально, не дублирование.

### Контракт API (зафиксирован)

**Эндпоинт:** `GET /api/v1/staff`
**Авторизация:** любая авторизованная роль (резидент, owner, tenant, staff, admin). Не путать с `GET /admin/staff` — тот только для админа и отдаёт счётчики заявок.

**Ответ — массив объектов с полями:**
- `id` — UUID сотрудника
- `full_name` — ФИО
- `email` — почта (нужна для контакта; фронт её не отображает, но контракт пусть будет полный)
- `phone` — телефон
- `specialty` — одно из `plumbing` / `electrical` / `cleaning` / `garbage` / `intercom` / `elevator`
- `work_schedule` — строка вида «Пн–Пт, 09:00–18:00»
- `description` — описание зоны ответственности (может быть пусто)
- `is_available` — bool, доступен ли сотрудник сейчас (можно вычислять — например `true` если у него нет активных заявок, либо вернуть просто `true` для MVP)

Формат полей совпадает с тем, что фронт уже использует (`StaffMember.fromMap`).

### Как протестировать (когда бэк готов)

1. Залогиниться админом, открыть «Сотрудники», добавить нового сотрудника специальности `electrical` (например `Иван Тестов`, `Пн–Пт, 09:00–18:00`).
2. Логаут → логин резидентом.
3. Открыть вкладку «Сервисы» → раздел «Службы ЖК».
4. ✅ Тапнуть «Электрик» → на детальной странице видны **актуальные** ФИО / телефон / часы добавленного сотрудника, описание и статус «Доступен».
5. На бэке временно убрать сотрудника специальности `plumbing`.
6. Свайп вниз для обновления.
7. ✅ Карточка «Сантехник» показывает «Сотрудник не назначен», серая точка «Недоступен».
8. **Edge case:** выключить бэк, открыть «Сервисы».
9. ✅ Все карточки в режиме «не назначен», сверху жёлтый баннер «Не удалось загрузить сотрудников».

---

---

## Фикс 6 — sensor-уведомления в истории резидента + иконки

**Файл Flutter:** `lib/pages/notifications_page.dart`
**Коммит Flutter:** `b301bd7`
**Бэк:** **нужна доработка** (см. ниже).

### Контекст
На скриншоте `fix_2.jpg` у админа в колокольчике видны и парковочные, и sensor-уведомления (тревога: пожар / затопление / задымление / возможное затопление). У резидента того же события — только парковочные, sensor-уведомления **не появляются**.

При этом сам **FCM-push на телефон жильца приходит** (это работает с IoT-фазы), но в БД в `notifications` для этого пользователя строка не создаётся. Поэтому когда житель открывает колокольчик и фронт дёргает `GET /notifications`, sensor-событий там нет.

### Что сделано на фронте
`lib/pages/notifications_page.dart` — добавлены иконки для двух «sensor»-видов уведомлений:
- `_KindIcon` теперь принимает опциональный `threatType` (из `item.data['threat_type']`).
- `sensor_alert` + `threat_type=FIRE/SMOKE` → 🔥 `local_fire_department` deepOrange.
- `sensor_alert` + `threat_type=WATER_LEAK/WATER` → 💧 `water_drop` blue.
- `sensor_alert` без threat_type → ⚠️ `warning_amber` orange.
- `sensor_offline` → 📶 `wifi_off` grey.

Это **не решает** саму проблему — лишь делает экран читаемее, когда бэк начнёт писать события. Без бэка ничего не изменится.

### Что нужно сделать Backend Claude

При создании sensor-уведомления (тревога датчика, изменение статуса админом, оффлайн-датчик и т.п.) запись в таблице `notifications` сейчас создаётся, судя по всему, **только админам**. Нужно, чтобы запись писалась также **жильцам того подъезда/этажа, где сработал датчик** (тем, кому уже улетает FCM-push).

Я не знаю, как у тебя устроены хэндлеры sensor-событий (`internal/handler/sensors.go` / `mqtt/client.go` / `sensor_events.go` — не уверен). У меня нет доступа к бэк-коду, поэтому опишу контракт со стороны фронта, а реализацию — на твоё усмотрение.

**Что фронт ожидает:**
- `GET /api/v1/notifications` для роли `resident`/`owner`/`tenant` возвращает в том числе sensor-события, относящиеся к этому пользователю (тот же контракт, что и для админа).
- Поля каждого item: `id`, `kind` (`sensor_alert` / `sensor_offline` / возможно другие), `title`, `body`, `data` (JSON), `read_at`, `created_at`.
- В `data` для `sensor_alert` фронт читает `threat_type` (`FIRE` или `WATER_LEAK`) — он уже есть в FCM-payload, который ты шлёшь жителям.
- Счётчик `GET /notifications/unread-count` должен учитывать эти sensor-уведомления.

**Что важно:**
- Адресация: жильцу нужно показывать события только тех датчиков, которые относятся к нему (его подъезд / этаж / квартира — у тебя есть какая-то логика, по которой ты решаешь, кому слать FCM; ту же логику применить к записи в `notifications`).
- Не дублировать: одно событие — одна запись на пользователя, а не одна на всех скопом. Если ты так уже делаешь для админа — продлить ту же модель на жильцов.
- Бэк уже умеет писать sensor-события админу (на скриншоте они есть) — значит код записи существует, нужно расширить аудиторию.

### Как протестировать (когда бэк готов)

1. Залогиниться жителем 2-го подъезда, 3-й этаж.
2. В Node-RED дёрнуть Water Alert 2/3 (или какую-то комбинацию подъезда/этажа, на которой стоит этот пользователь).
3. ✅ На телефон приходит FCM-push (это уже работает сейчас).
4. Открыть колокольчик в приложении.
5. ✅ В истории видна новая запись «Тревога: возможное затопление» с синей иконкой капли.
6. Админ подтверждает тревогу (CONFIRMED).
7. У жителя свайп вниз для обновления.
8. ✅ Появилась вторая запись «Тревога: затопление» от админа.
9. Тапнуть на любую запись → она помечается прочитанной (бейдж пропадает).
10. **Edge case:** залогиниться жителем 1-го подъезда. Триггерить датчик 2-го подъезда.
11. ✅ У этого жителя в истории **ничего не появилось** (адресация работает).

### Как протестировать (только фронт-часть, без бэка)

Если хочется проверить иконки прямо сейчас — залогиниться **админом** (у него sensor-уведомления уже пишутся в БД). Триггерить датчик. Открыть колокольчик.
- ✅ У записи «Тревога: пожар» — оранжевая иконка огня вместо синего колокольчика.
- ✅ У «Тревога: затопление» — синяя капля.
- ✅ У «Тревога: возможное затопление / задымление» — соответствующие иконки.

---

## Общие чек-листы (после ВСЕХ фиксов)

### Регрессии — что не должно сломаться
- [ ] Логин/регистрация работают как раньше.
- [ ] Существующие гостевые пропуска без `parking_spot_id` (созданные до Фикса 1) **не падают** в UI.
- [ ] FCM-уведомления с `kind=parking_alert` (permanent-сценарий) приходят и обрабатываются.
- [ ] Старая логика «Сообщить администратору» в `guests_page.dart` для **active** пропусков работает.
- [ ] Список сотрудников `GET /admin/staff` грузится корректно после добавления нового сотрудника.

### Финальная демо-цепочка для защиты (3 минуты)
1. **Auth:** регистрация в 4 шага → логин.
2. **IoT:** Node-RED Water Alert → Dashboard «Тревога!» → принять → подтвердить → решить.
3. **Parking attempts:** 3 отказа резиденту → апелляция → resolve.
4. **Guest QR:** создать пропуск с местом → 1 скан → «На территории» → 2 скан → «Использован».
5. **Admin verification:** просмотр документа → одобрение.
6. **(Новое)** **Создание сотрудника:** админ → Сотрудники → `+` → форма → создание → логин нового сотрудника.

---

## Фикс 5 (дополнение 2026-05-27) — overflow в длинных именах сотрудников

**Файл:** `lib/pages/service_requests_page.dart`
**Коммит Flutter:** `90dc3e8`
**Бэк:** изменений нет.

### Что сделано
- В карточке заявки при отображении имени назначенного сотрудника добавлен `Expanded` + `TextOverflow.ellipsis`.
- Длинные ФИО (типо «Виктор Сергеевич Александровский») теперь обрезаются с многоточием вместо переполнения вправо.
- Добавлено `maxLines: 1` для предотвращения переноса текста на несколько строк.

### Как протестировать
1. Админ создаёт заявку и назначает сотрудника с длинным ФИ (16+ символов).
2. Открыть список заявок.
3. ✅ На карточке видно имя сотрудника, обрезанное с многоточием (типо «Виктор Сергеевич...»).
4. ✅ Текст НЕ выходит за границы карточки.

---

## Фикс 3 (новый, 2026-05-27) — сотрудник меняет пароль при первом входе

**Файлы Flutter:** `lib/pages/change_password_on_first_login_page.dart`, `lib/services/auth_service.dart`, `lib/pages/login_page.dart`
**Коммит Flutter:** `81bff41`
**Бэк:** требуется реализация (см. ниже).

### Что сделано на фронте
1. **Новый класс `LoginResult`** (sealed class) с тремя вариантами: успех, ошибка, требуется смена пароля.
2. **Метод `AuthService.login()`** теперь возвращает `LoginResult` вместо простой строки ошибки.
3. **Проверка статуса** при логине: если `status == 'password_change_required'`, показывается блокирующий экран.
4. **Экран `ChangePasswordOnFirstLoginPage`**: форма для ввода нового пароля с подтверждением. 
   - Использует временный токен из ответа логина.
   - POST запрос на `/auth/change-password-first-login` с новым паролем.
   - После успеха получает новый полноценный токен и возвращает в приложение.
5. **Обновлена `login_page.dart`**: обработка трёх случаев LoginResult через pattern matching.

### Что нужно сделать Backend Claude

При логине сотрудника с временным паролем (созданного через `POST /admin/staff`):

```
POST /auth/login
{
  "email": "staff@example.com",
  "password": "временный_пароль"
}
```

Вернуть специальный ответ **вместо обычного** (не 401, а 200):

```json
{
  "status": "password_change_required",
  "user": {
    "id": "uuid",
    "email": "staff@example.com",
    "role": "staff"
  },
  "token": "временный_jwt_токен_на_15_минут"
}
```

Затем фронт отправит:

```
POST /auth/change-password-first-login
Authorization: Bearer временный_jwt_токен

{
  "new_password": "новый_пароль"
}
```

Бэк должен:
- Проверить, что токен временный (или есть флаг в нём).
- Обновить пароль в БД.
- Вернуть новый полноценный JWT токен.

### Как протестировать

1. Админ создаёт сотрудника через «Сотрудники» → `+`.
2. В ответе админ видит временный пароль (или получает по почте — на твоё усмотрение).
3. Сотрудник логинится с временным паролем.
4. ✅ Приложение показывает экран «Смена пароля при первом входе».
5. Сотрудник вводит новый пароль (дважды для подтверждения).
6. ✅ Нажимает «Изменить пароль» → возвращается в приложение с полным доступом.
7. Логаут → попробовать логин со старым временным паролем.
8. ✅ 401 Unauthorized (временный пароль больше не работает).

---

## Фикс 2 (новый, 2026-05-27) — смена пароля в профиле + меню настроек

**Файлы Flutter:** `lib/pages/change_password_dialog.dart`, `lib/pages/profile_page.dart`
**Коммит Flutter:** `6db4652`
**Бэк:** требуется реализация.

### Что сделано на фронте
1. **Кнопка «Настройки»** (иконка gear) в AppBar профиля — видна только для авторизованных пользователей.
2. **Меню с двумя опциями:**
   - «Сменить пароль» → диалог смены
   - «Выйти» → логаут
3. **Диалог `ChangePasswordDialog`**: три поля:
   - Текущий пароль (для верификации)
   - Новый пароль
   - Подтверждение пароля
4. **POST запрос** на `/auth/change-password` с текущим и новым паролем.
5. **Валидация фронта**: совпадение паролей, минимум 6 символов.
6. **Удалена кнопка «Выйти»** внизу страницы (переехала в меню).

### Что нужно сделать Backend Claude

Добавить эндпоинт:

```
POST /api/v1/auth/change-password

{
  "current_password": "oldpass123",
  "new_password": "newpass456"
}
```

Бэк должен:
- Проверить текущий пароль (bcrypt-сравнение).
- Если неправильный → `401 Unauthorized`.
- Проверить, что новый пароль ≠ текущему.
- Проверить минимум 6 символов.
- Обновить пароль в БД.
- Вернуть **новый JWT токен** (в поле `token`).

### Как протестировать

1. Залогиниться под любым пользователем → Профиль.
2. ✅ В AppBar видна иконка шестеренки.
3. Тапнуть → меню с двумя опциями.
4. Выбрать «Сменить пароль».
5. ✅ Диалог с тремя полями пароля.
6. Ввести текущий пароль **неправильно**.
7. ✅ После submit — снэкбар «Текущий пароль неправильный».
8. Ввести корректный текущий пароль + новый (разные, ≥6 символов) + подтверждение.
9. ✅ Диалог закрывается, снэкбар «Пароль успешно изменён».
10. Логаут → попробовать логин со старым паролем.
11. ✅ 401 (старый пароль не работает).
12. Логин с новым паролем.
13. ✅ Успех.

---

## Фикс 1 (новый, 2026-05-27) — восстановление пароля на странице логина

**Файлы Flutter:** `lib/pages/forgot_password_page.dart`, `lib/pages/login_page.dart`
**Коммит Flutter:** `d4d05da`
**Бэк:** требуется реализация.

### Что сделано на фронте
1. **Кнопка «Проблемы с авторизацией?»** на странице логина.
2. **Экран `ForgotPasswordPage`** с двумя шагами (управляются через StatefulWidget):
   - **Шаг 1:** ввод email → POST `/auth/forgot-password` → сообщение «Код отправлен на почту».
   - **Шаг 2:** ввод 6-значного кода + новый пароль (дважды) → POST `/auth/reset-password`.
3. **Валидация фронта**: 
   - Код должен быть ровно 6 цифр.
   - Пароль ≥6 символов.
   - Пароли совпадают.
4. **После успеха шага 2**: возврат на страницу логина.

### Что нужно сделать Backend Claude

**Шаг 1 — инициация:**

```
POST /api/v1/auth/forgot-password

{
  "email": "resident@example.com"
}
```

Бэк должен:
- Вернуть `200 OK` с `{message: "Код отправлен на почту"}` (даже если email не существует, для безопасности).
- Сгенерировать 6-значный код.
- Сохранить код в Redis с TTL **15 минут**.
- Отправить код по почте (Gmail SMTP).

**Шаг 2 — сброс пароля:**

```
POST /api/v1/auth/reset-password

{
  "email": "resident@example.com",
  "code": "123456",
  "new_password": "newpass789"
}
```

Бэк должен:
- Проверить код в Redis.
- Если неправильный или истёк → `400 Bad Request`.
- Обновить пароль в БД.
- **Удалить код из Redis** (одноразовый).
- Вернуть `200 OK` с новым JWT токеном.

### Как протестировать

1. На странице логина нажать «Проблемы с авторизацией?».
2. ✅ Открывается `ForgotPasswordPage`.
3. Ввести email существующего пользователя.
4. ✅ Снэкбар «Код отправлен на почту», переход на шаг 2.
5. Ввести неправильный код (типо 000000).
6. ✅ Снэкбар «Неверный или истекший код» (400).
7. Ввести правильный код из письма + новый пароль (2 раза).
8. ✅ Снэкбар «Пароль успешно изменён», возврат на логин.
9. Логин со старым паролем.
10. ✅ 401.
11. Логин с новым паролем.
12. ✅ Успех.

---

## Фикс 6 (новый, 2026-05-27) — новости в ЖК

**Файлы Flutter:** `lib/pages/news_page.dart`, `lib/pages/dashboard_page.dart`
**Коммит Flutter:** `0fbee5e`
**Бэк:** требуется реализация.

### Что сделано на фронте
1. **Страница `NewsPage`** со списком новостей.
2. **Модель `News`**: id, title, body, author, entrance (nullable), created_at, is_pinned.
3. **Функционал для всех пользователей:**
   - `GET /api/v1/news` с автофильтрацией по подъезду.
   - Сортировка: закреплённые первыми (is_pinned=true), потом по дате (свежие вверху).
4. **Функционал для админа только:**
   - Кнопка `+` в AppBar для создания новости.
   - Меню (три точки) в каждой карточке: редактировать, удалить.
5. **Диалоги:**
   - **`_CreateNewsSheet`**: заголовок, текст, чекбоксы «Закрепить» и «Для всех подъездов» → POST.
   - **`_EditNewsSheet`**: отредактировать существующую новость → PUT.
6. **Интеграция в меню:**
   - Для админа добавлена новая вкладка (6-я) в BottomNavigationBar: "Новости".
   - Обновлён `_pageCountForRole` для админа на 6 страниц.

### Что нужно сделать Backend Claude

**Таблица (если ещё нет):**

```sql
CREATE TABLE news (
  id UUID PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  body TEXT NOT NULL,
  author_id UUID NOT NULL,
  entrance_id UUID NULLABLE,
  is_pinned BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

**Эндпоинты:**

1. **GET /api/v1/news** — для всех авторизованных

Возвращает список новостей с автофильтром:
- Если у пользователя есть `entrance_id` → `WHERE entrance_id IS NULL OR entrance_id = user_entrance_id`.
- Иначе (админ, сотрудник) → все новости.
- Сортировка: `ORDER BY is_pinned DESC, created_at DESC`.

Ответ:
```json
[
  {
    "id": "uuid1",
    "title": "Плановое отключение воды",
    "body": "2 июня с 09:00 до 15:00...",
    "author": "Иван Петров",
    "entrance": "Подъезд 1" (или null),
    "created_at": "2026-05-27T10:30:00Z",
    "is_pinned": true
  }
]
```

2. **POST /api/v1/admin/news** — только админ

```json
{
  "title": "...",
  "body": "...",
  "entrance_id": null,
  "is_pinned": false
}
```

- Автоматически берёшь `author_id` из JWT токена.
- Вернуть `201 Created`.

3. **PUT /api/v1/admin/news/:id** — только админ, только свои новости

```json
{
  "title": "...",
  "body": "...",
  "is_pinned": false
}
```

- Проверь, что `author_id` новости == текущему пользователю (или админ может менять чужие — на твоё усмотрение).
- Обновить `updated_at`.

4. **DELETE /api/v1/admin/news/:id** — только админ

- Удалить новость.
- `200 OK` или `204 No Content`.

### Как протестировать

1. Админ открывает Дашборд → вкладка «Новости» (6-я).
2. ✅ Список новостей (если будут).
3. Тапнуть кнопку `+` в AppBar.
4. ✅ Открывается `_CreateNewsSheet`.
5. Заполнить: заголовок «Плановое отключение воды», текст, чекбокс «Закрепить».
6. ✅ После создания список обновляется, новость появляется первой (так как закреплена).
7. Тапнуть на новость → три точки → «Редактировать».
8. ✅ Открывается `_EditNewsSheet` с заполненными полями.
9. Изменить текст, нажать «Обновить».
10. ✅ Список обновляется.
11. Три точки → «Удалить» → подтверждение.
12. ✅ Новость удалена.
13. Резидент логинится → открывает **любой другой экран** и снова туда входит (на NewsPage нет для резидента доступа прямого, нужна интеграция в главное меню или доступ из Дашборда).
14. *Альтернатива:* если резидент может видеть новости на главной странице или в отдельном разделе → проверить, что видит только свои подъезд + общие новости.

---

## Журнал коммитов Flutter

| Дата | Коммит | Что |
|---|---|---|
| 2026-05-27 | `4224060` | Add Flutter demo notes for diploma defense |
| 2026-05-27 | `819931f` | Merge teammate/main: parking attempts, appeal flow, guest spot binding |
| 2026-05-27 | `f15ddff` | Фиксы 1 и 3: обязательное место + умная логика «Сообщить» |
| 2026-05-27 | `0e6edfa` | Фикс 2: покрытие `parking_no_permit` в notifications_service |
| 2026-05-27 | `5238236` | Фикс 4: форма создания сотрудника в admin_staff_page |
| 2026-05-27 | `9952cd1` | Add FIXES.md tracking all post-merge fixes |
| 2026-05-27 | `204694c` | Фикс 5: динамический список сотрудников в «Сервисы» |
| 2026-05-27 | `fb34f2b` | FIXES.md: раздел Фикс 5 |
| 2026-05-27 | `5334907` | FIXES.md: Фиксы 4-5 закрыты на бэке (`c3309ca`) |
| 2026-05-27 | `b301bd7` | Фикс 6 (фронт-часть): иконки sensor_alert / sensor_offline |
| 2026-05-27 | `35ae0ab` | Fix: correct FIXES.md header and update API server host |
| 2026-05-27 | `1627de8` | Merge branch 'main' (pull from remote) |
| 2026-05-27 | `90dc3e8` | Fix: prevent text overflow in staff member names in service requests |
| 2026-05-27 | `81bff41` | Implement Fix 3: Staff password change on first login |
| 2026-05-27 | `6db4652` | Implement Fix 2: Profile settings menu (logout, change password) |
| 2026-05-27 | `d4d05da` | Implement Fix 1: Password recovery flow on login screen |
| 2026-05-27 | `0fbee5e` | Implement Fix 6: News in residential complex (admin and residents) |

## Журнал коммитов Backend (по словам Backend Claude)

| Дата | Коммит | Что |
|---|---|---|
| 2026-05-27 | `094ba13` | Merge: 3 попытки, апелляция, parking_permit_status, гостевой parking_spot_id |
| 2026-05-27 | `8721742` | Фикс 2: переименование `parking_alert` → `parking_no_permit` (mqtt/client.go) |
| 2026-05-27 | `c3309ca` | Фиксы 4+5: `POST /admin/staff` + `GET /staff` + миграция 014 |
| TBD | TBD | Фикс 6: sensor-уведомления для жильцов |

---

## Фикс 2.2 & 2.3 (2026-05-28) — Несколько сотрудников на специальность + правильная фильтрация заявок ✅ ГОТОВ

**Файл Flutter:** `lib/pages/services_page.dart`
**Коммит Flutter:** (в работе)
**Бэк:** требуется доработка (см. ниже).

### Контекст
При наличии двух или более сотрудников одной специальности (например, два Сантехника) на странице «Сервисы»:
- **Фикс 2.2:** Раньше отображался только первый сотрудник из-за `putIfAbsent()`, который хранит одного per specialty.
- **Фикс 2.3:** Заявки не фильтровались по назначенному сотруднику (`assigned_to`), что приводило к смешиванию заявок разных сотрудников.

### Что сделано на фронте

1. **Изменена структура данных в `_ServicesPageState._load()`:**
   - Было: `Map<String, StaffMember>` (один сотрудник per specialty)
   - Стало: `Map<String, List<StaffMember>>` (все сотрудники per specialty)
   - Новое поле: `late Map<String, List<StaffMember>> _staffBySpecialty = {}`

2. **`ServiceDetailPage` теперь принимает список сотрудников:**
   - Добавлен параметр `final List<StaffMember>? staffList`
   - При наличии нескольких сотрудников показываются горизонтальные вкладки (FilterChip) с выбором

3. **Фильтрация заявок по выбранному сотруднику:**
   - Добавлено поле `_selectedStaffId` для отслеживания выбранного сотрудника
   - При загрузке заявок проверяется условие:
     ```dart
     if (widget.staffList != null && widget.staffList!.length > 1 && _selectedStaffId != null) {
       filtered = filtered.where((r) => r.assignedTo == _selectedStaffId).toList();
     }
     ```
   - Каждый сотрудник видит только свои заявки по полю `assigned_to`

4. **Обновлена модель `_SvcRequest`:**
   - Добавлено поле `final String? assignedTo`
   - При парсинге из API: `assignedTo: m['assigned_to'] as String?`

5. **UI улучшения:**
   - При наличии нескольких сотрудников над карточкой сотрудника показываются FilterChip-вкладки
   - При смене вкладки автоматически перезагружаются заявки
   - Карточка сотрудника всегда показывает данные выбранного сотрудника (ФИО, телефон, статус, описание)

### Что нужно сделать Backend Claude

1. **Убедиться, что в ответе `GET /service-requests` поле `assigned_to` содержит ID сотрудника:**
   ```json
   {
     "id": "request_uuid",
     "category": "Протечка",
     "description": "Утечка под раковиной",
     "status": "in_progress",
     "assigned_to": "staff_uuid",
     "created_at": "2026-05-28T10:00:00Z",
     "photos": ["url1", "url2"]
   }
   ```
   - Если `assigned_to` сейчас нет в ответе — добавить.
   - Если поле есть в структуре заявки в БД — просто убедиться, что оно возвращается в API.

2. **Убедиться, что `GET /staff` возвращает поле `id` для каждого сотрудника:**
   - Фронт использует `staff.id` для фильтрации, поэтому это поле обязательно.

### Как протестировать

**Предусловие:** в системе два сотрудника специальности `plumbing` (два Сантехника).

1. Резидент логинится → Сервисы.
2. Тапнуть на карточку «Сантехник».
3. ✅ Над карточкой сотрудника видны **две горизонтальные вкладки** (FilterChip) с именами обоих сотрудников.
4. **Первый сотрудник выбран по умолчанию** → показываются его данные (телефон, часы, описание).
5. ✅ Список заявок ниже содержит **только заявки этого сотрудника** (фильтр по `assigned_to`).
6. Тапнуть на вторую вкладку.
7. ✅ Карточка обновляется с данными второго сотрудника.
8. ✅ Список заявок полностью перезагружается → содержит **только заявки второго сотрудника**.
9. **Edge case:** если у одного из них 0 заявок → список пуст или показывает «Заявок пока нет».
10. Свайп вниз для обновления.
11. ✅ Данные актуальны, фильтрация всё ещё работает.

**Edge case (когда сотрудник один):**
1. Если у специальности `electrical` только один сотрудник.
2. Открыть карточку «Электрик».
3. ✅ Вкладки с выбором сотрудника **НЕ видны** (условие `hasMultipleStaff`).
4. ✅ Заявки показываются **все** по категории (фильтр по `assigned_to` не применяется).

---

**Поддерживается Flutter-Claude в сессии Мансура. При появлении нового фикса —
дописывать новую секцию по тому же шаблону: что сделано / что нужно от бэка / как протестировать.**
